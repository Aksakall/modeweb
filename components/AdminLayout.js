export function AdminLayout(content,title='Yönetim Paneli'){
  const links=[['dashboard','Dashboard'],['products','Ürünler'],['categories','Kategoriler'],['orders','Siparişler'],['customers','Müşteriler'],['newsletter','E-bülten'],['settings','Site Ayarları']];
  return `<div class="admin-shell"><aside><a class="admin-brand" href="admin.html">Sıla Sarıoğlu<small>Yönetim</small></a><nav>${links.map(([id,label],index)=>`<a href="#${id}" data-admin-section="${id}" class="${index===0?'is-active':''}">${label}</a>`).join('')}</nav><button class="admin-logout" id="admin-logout" type="button">Çıkış Yap</button></aside><main><header><div><p class="admin-eyebrow">Butik Operasyonları</p><h1 id="admin-title">${title}</h1></div><span>Güvenli Yönetim</span></header><div class="admin-status" id="admin-status" role="status" hidden></div><div id="admin-content">${content}</div></main></div><div id="admin-modal-root"></div>`;
}
