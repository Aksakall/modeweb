# Backend TODO

- Production kalıcı kullanıcı sepeti istenirse `Cart` ve `CartItem` Prisma modelleri eklenecek.
- Mevcut backend cart endpointleri sadece giriş yapan kullanıcı içindir ve geçici olarak memory `Map` kullanır.
- Misafir sepeti frontend `localStorage` içinde kalır; login/register sonrasında backend cart endpointine merge edilir.
